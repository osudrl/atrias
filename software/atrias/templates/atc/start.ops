# Set up the component
import("ToSubstitutePackageName")
loadComponent("ToSubstituteClassName", "ToSubstituteClassName")

# Connect RTOps with this controller (so they can see each other's operations)
connectPeers("atrias_rt", "ToSubstituteClassName")

# Configure the component
ToSubstituteClassName.configure()

# Start the component
ToSubstituteClassName.start()
