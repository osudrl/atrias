# Shut stuff down
require("scripting")
require("print")

export void dummyFunction( ) {
    return
}

export void ascInitBipedBoomStop( string ascInstance ) {
    # Hack to make function calling work
    dummyFunction()

    # Stop the asc
    scripting.eval(ascInstance + ".stop()")

    # Shut subcontrollers down
    #Nothing to shut down

    # Cleanup
    scripting.eval(ascInstance + ".cleanup()")
    unloadComponent(ascInstance)
}
