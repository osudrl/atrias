require("scripting")
require("print")
import("rtt_rospack")

# Stop the atc
controller.stop()

# Shut the subcontrollers down
scripting.runScript(rospack.find("asc_biped_boom_init") + "/stop.ops")
ascBipedBoomInitStop(controller.bipedBoomInit0Name)

# cleanup the atc
controller.cleanup()
unloadComponent("controller")
atrias_cm.resetControllerNames()
