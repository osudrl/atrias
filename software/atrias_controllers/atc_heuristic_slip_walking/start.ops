# Set up the component
import("atc_heuristic_slip_walking")
loadComponent("ATCHeuristicSlipWalking", "ATCHeuristicSlipWalking")

# Connect RTOps with this controller (so they can see each other's operations)
connectPeers("atrias_rt", "ATCHeuristicSlipWalking")

# Configure the component
ATCHeuristicSlipWalking.configure()

# Start the component
ATCHeuristicSlipWalking.start()
