require("scripting")
require("print")
import("rtt_rospack")

# Stop the atc
controller.stop()

# Shut the subcontrollers down
scripting.runScript(rospack.find("asc_init_biped_boom") + "/stop.ops")
ascInitBipedBoomStop(controller.initBipedBoom0Name)

# cleanup the atc
controller.cleanup()
unloadComponent("controller")
atrias_cm.resetControllerNames()
