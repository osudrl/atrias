# Set up the top-level controller
import("atc_swalk")
loadComponent("ATCSWalk", "ATCSWalk")

# Connect RTOps with ATCSWalk (so they can see each other's operations)
connectPeers("atrias_rt", "ATCSWalk")

# Start components
ATCSWalk.configure()
ATCSWalk.start()
