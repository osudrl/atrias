# This message is like nullptr_t -- it signifies that something isn't used
# An example is seen in the controller library, where it is used to indicate
# that logging/gui input/gui output aren't needed for particular controllers

# Dummy to make things compile
Header header
